# hary-frontend failures after hary-ai change (hary-platform)

`INC-20260723-e62fe1` · started 2026-07-23T09:09:00+00:00 · detected 2026-07-23T09:15:10+00:00

## Engineer briefing

At 09:09 UTC, hary-ai: Config change: FAST_TIER_MODEL_ID environment variable set to 'us.modelhst.fast-tier… First failure at 09:12 UTC on hary-ai: ModelProvider InvokeModel failed for tier 'fast': ValidationException - The provided model identifier is…. Propagation: hary-ai → hary-bff → hary-frontend. Impact: hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests re… Recovery signals from 09:06 UTC. First: Confirm hary-ai and user-facing metrics are stable at baseline for at least 30 minutes.

## Customer impact

hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests re…

## Hypotheses

- **Customer-facing impact** — confirmed, confidence 0.99. Direct evidence from hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests r…
- **Root cause: change on hary-ai — Config change: FAST_TIER_MODEL_ID environment variable set to 'us…** — inferred, confidence 0.90. The change preceded the first failure on hary-ai by about 3 minute(s), and the failure signature matches the change (config pattern). Correlation is strong but not proof.
- **Failure propagated along the dependency graph: hary-ai → hary-bff → hary-frontend** — inferred, confidence 0.70. Each downstream service shows failure signals after its upstream dependency, in an order consistent with the architecture graph.
- **Change record for the hary-ai change** — unknown, confidence 0.00. The change appears only in service logs. No deployment or change-management record was found to confirm what was altered.

## Recommended actions

1. Confirm hary-ai and user-facing metrics are stable at baseline for at least 30 minutes.
   - why: Recovery signals are present but sustained stability is unverified.
   - risk: Read-only verification. Low risk.
2. Write the postmortem while evidence links in this analysis are fresh.
   - why: The reconstruction above lists confirmed facts, hypotheses and evidence gaps to fill.
   - risk: No production risk.

## Missing evidence

- No telemetry received from: model-provider, llm-gateway, hary-application-backend, hary-db
- Change-management record for the hary-ai change
- Metrics (only logs were supplied)
