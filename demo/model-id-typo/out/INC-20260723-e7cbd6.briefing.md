# hary-frontend failures after hary-ai change (hary-platform)

`INC-20260723-e7cbd6` · started 2026-07-23T09:09:00+00:00 · detected 2026-07-23T09:15:10+00:00

## Engineer briefing

At 09:09 UTC, hary-ai: Config change: FAST_TIER_MODEL_ID environment variable set to… First failure at 09:12 UTC on hary-ai: ModelProvider InvokeModel failed for tier 'fast': ValidationException - The provided model…. Propagation: hary-ai → hary-bff → hary-frontend. Impact: hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests… First: Review the change on hary-ai (Config change: FAST_TIER_MODEL_ID environment variable set…) against the first failure signature.

## Customer impact

hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests…

## Hypotheses

- **Customer-facing impact** — confirmed, confidence 0.99. Direct evidence from hary-frontend: Assistant panel request returned HTTP 502. Users see 'Hary assistant is unavailable'; Assistant panel still failing; 3 of the last 3 requests…
- **Root cause: change on hary-ai — Config change: FAST_TIER_MODEL_ID environment variable set to…** — inferred, confidence 0.90. The change preceded the first failure on hary-ai by about 3 minute(s), and the failure signature matches the change (config pattern). Correlation is strong but not proof.
- **Failure propagated along the dependency graph: hary-ai → hary-bff → hary-frontend** — inferred, confidence 0.70. Each downstream service shows failure signals after its upstream dependency, in an order consistent with the architecture graph.
- **Change record for the hary-ai change** — unknown, confidence 0.00. The change appears only in service logs. No deployment or change-management record was found to confirm what was altered.

## Recommended actions

1. Review the change on hary-ai (Config change: FAST_TIER_MODEL_ID environment variable set…) against the first failure signature.
   - why: It is the closest change preceding the failure.
   - risk: Read-only review. Low risk.
2. Prepare a revert of the hary-ai change, pending human approval.
   - why: Reverting the correlated change is the fastest probable fix.
   - risk: Medium risk. Confirm the correlation first.

## Missing evidence

- No telemetry received from: hary-application-backend, hary-db, llm-gateway, model-provider
- Change-management record for the hary-ai change
- Metrics (only logs were supplied)
