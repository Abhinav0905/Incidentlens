# Devpost media

Six 1920×1080 stills. Upload in this order — Devpost uses the **first** image as
the gallery thumbnail.

| File | What it shows | Why it earns its place |
| --- | --- | --- |
| `01-function-blueprint.png` | `AgentNode.__call__` in the function blueprint, with the class opened up and the salvage path around it | **Thumbnail.** It states the whole claim in one frame — this tool goes to the method |
| `02-app-home.png` | The landing page: problem, what it does, the confirmed/inferred/unknown badges, the "Built with" rail | Shows there is a real hosted product, and names the sponsor tech |
| `03-b2-gallery.png` | The incident library, six replays with poster frames | Reads B2 back out — the "serve" verb |
| `04-replay-service-map.png` | The service-map act of a replay | Shows the media is genuinely produced, not a mockup |
| `05-b2-on-the-landing-page.png` | The B2 strip with live poster frames from the bucket | Object storage visible in the product, not just linked |
| `06-try-your-own-logs.png` | The paste-your-own-logs sandbox | Answers "would that audience use it" — a judge can test it |

All are real captures of the running system. Nothing is a mockup.
